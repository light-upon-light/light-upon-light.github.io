# Messenger page overhaul

This folder contains the completed split of the original `messenger.md` into one concise main argument and fifteen in-depth evidence pages.

## Deployment

- Replace the existing `/messenger` source file with `messenger.md`.
- Add the fifteen numbered Markdown files to the site. Their front matter already defines their `/messenger/.../` permalinks.
- `11-sources.md` is the centralized bibliography and source-methodology page. Footnote references on the other pages point to its `#ref-*` anchors.

## Verification

- `MIGRATION_LEDGER.md` records the destination of every original section.
- `PRESERVATION_AUDIT.md` records the automated content-preservation check.
- `AUDIT.txt` records basic link/reference checks.

The original long page remains the basis for the secondary pages; the new `messenger.md` is intentionally a fresh, shorter cumulative argument.
